---
import AppLayout from "../layouts/AppLayout.astro";
---

<AppLayout title="Punto de Venta">
    <div class="pos-container">
        <div class="pos-grid">
            <div class="pos-products">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Productos</h3>
                        <div class="search-container">
                            <input
                                type="text"
                                id="product-search"
                                class="form-input search-input"
                                placeholder="🔍 Buscar producto..."
                            />
                        </div>
                    </div>
                    <div id="products-list" class="products-grid"></div>
                </div>
            </div>

            <div class="pos-cart">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Carrito</h3>
                    </div>

                    <div id="cart-items" class="cart-items"></div>

                    <div class="cart-summary">
                        <div class="summary-row">
                            <span>Subtotal:</span>
                            <span id="subtotal">$0.00</span>
                        </div>
                        <div class="summary-row">
                            <span>Descuento:</span>
                            <input
                                type="number"
                                id="discount"
                                class="form-input"
                                value="0"
                                min="0"
                                step="0.01"
                                style="width: 100px; text-align: right;"
                            />
                        </div>
                        <div class="summary-row total">
                            <span>Total:</span>
                            <span id="total">$0.00</span>
                        </div>
                    </div>

                    <div class="payment-methods">
                        <label class="form-label">Método de Pago:</label>
                        <select id="payment-method" class="form-select">
                            <option value="EFECTIVO">Efectivo</option>
                            <option value="DEBITO">Débito</option>
                            <option value="CREDITO">Crédito</option>
                            <option value="TRANSFERENCIA">Transferencia</option>
                        </select>
                    </div>

                    <button
                        id="complete-sale-btn"
                        class="btn btn-primary"
                        style="width: 100%;"
                        disabled>
                        Completar Venta
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal del Ticket -->
    <div id="ticket-modal" class="modal-overlay" style="display: none;">
        <div class="modal-content ticket-modal">
            <button class="modal-close" id="close-ticket">×</button>
            <div id="ticket-content"></div>
            <div class="ticket-actions">
                <button class="btn btn-secondary" id="print-ticket"
                    >🖨️ Imprimir</button
                >
                <button class="btn btn-primary" id="new-sale"
                    >Nueva Venta</button
                >
            </div>
        </div>
    </div>
</AppLayout>

<style>
    .pos-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 1.5rem;
    }

    .search-container {
        margin-top: 1rem;
        position: relative;
    }

    .search-input {
        width: 100%;
        padding-left: 1rem;
        font-size: 0.9375rem;
    }

    .search-input:focus {
        padding-left: 1rem;
    }

    .products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
        gap: 1px;
        max-height: 600px;
        overflow-y: auto;
        overflow-x: visible;
        padding: 1px;
        background-color: #cbd5e1;
        border-radius: 1rem;
    }

    .products-grid:empty::after {
        content: "🔍 No se encontraron productos";
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 200px;
        color: var(--text-light);
        font-size: 1rem;
        grid-column: 1 / -1;
    }

    .product-card.product-added {
        animation: productAddedPulse 0.6s ease;
    }

    @keyframes productAddedPulse {
        0% {
            transform: scale(1);
            box-shadow: var(--shadow-sm);
        }
        50% {
            transform: scale(1.08);
            box-shadow: 0 0 30px rgba(16, 185, 129, 0.5);
            border-color: var(--success-color);
        }
        100% {
            transform: scale(1);
            box-shadow: var(--shadow-sm);
        }
    }

    .product-name {
        font-weight: 700;
        font-size: 0.9375rem;
        margin-bottom: 0.75rem;
        color: #0f172a;
        line-height: 1.3;
        letter-spacing: -0.01em;
        min-height: 2.6em;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    .product-price {
        color: #6366f1;
        font-weight: 800;
        font-size: 1.25rem;
        margin-bottom: 0.5rem;
        letter-spacing: -0.02em;
    }

    .product-stock {
        font-size: 0.8125rem;
        color: #475569;
        font-weight: 500;
        display: flex;
        align-items: center;
        gap: 0.25rem;
    }

    .product-stock::before {
        content: "📦";
        font-size: 0.875rem;
    }

    .cart-items {
        max-height: 300px;
        overflow-y: auto;
        margin-bottom: 1rem;
        background: var(--bg-secondary);
        border-radius: var(--radius);
        padding: 0.5rem;
    }

    .cart-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem;
        border-bottom: 1px solid var(--border-light);
        background: white;
        margin-bottom: 0.5rem;
        border-radius: var(--radius);
        transition: var(--transition);
    }

    .cart-item:last-child {
        margin-bottom: 0;
    }

    .cart-item:hover {
        background: rgba(99, 102, 241, 0.03);
        box-shadow: var(--shadow-sm);
    }

    .cart-item-info {
        flex: 1;
    }

    .cart-item-name {
        font-weight: 500;
        font-size: 0.875rem;
    }

    .cart-item-price {
        font-size: 0.8125rem;
        color: var(--text-light);
    }

    .cart-item-qty {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .qty-btn {
        width: 28px;
        height: 28px;
        border: 2px solid var(--border-color);
        background: white;
        border-radius: var(--radius-sm);
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.875rem;
        font-weight: 700;
        transition: var(--transition);
        user-select: none;
    }

    .qty-btn:hover {
        border-color: var(--primary-color);
        background: var(--primary-color);
        color: white;
        transform: scale(1.1);
    }

    .qty-btn:active {
        transform: scale(0.95);
    }

    .cart-summary {
        border-top: 2px solid var(--border-color);
        padding-top: 1rem;
        margin-bottom: 1rem;
    }

    .summary-row {
        display: flex;
        justify-content: space-between;
        margin-bottom: 0.5rem;
        font-size: 0.875rem;
    }

    .summary-row.total {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--primary-color);
        margin-top: 0.5rem;
    }

    .payment-methods {
        margin-bottom: 1rem;
    }

    .empty-cart {
        text-align: center;
        padding: 3rem 1rem;
    }

    .empty-cart-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        opacity: 0.3;
    }

    .empty-cart-text {
        font-size: 1.125rem;
        font-weight: 600;
        color: var(--text-secondary);
        margin-bottom: 0.5rem;
    }

    .empty-cart-hint {
        font-size: 0.875rem;
        color: var(--text-light);
    }

    @media (max-width: 968px) {
        .pos-grid {
            grid-template-columns: 1fr;
        }
    }

    .modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.7);
        backdrop-filter: blur(4px);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 1000;
        animation: fadeIn 0.2s ease;
    }

    .modal-content {
        background: white;
        border-radius: 1rem;
        max-width: 90%;
        max-height: 90vh;
        overflow-y: auto;
        position: relative;
        animation: slideDown 0.3s ease;
    }

    .ticket-modal {
        max-width: 400px;
        padding: 2rem;
    }

    .modal-close {
        position: absolute;
        top: 1rem;
        right: 1rem;
        background: none;
        border: none;
        font-size: 2rem;
        cursor: pointer;
        color: #64748b;
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        transition: all 0.2s;
    }

    .modal-close:hover {
        background: #f1f5f9;
        color: #0f172a;
        transform: scale(1.1);
    }

    .ticket {
        font-family: "Courier New", "Courier", monospace;
        padding: 1.75rem;
        background: white;
        border: 2px dashed #cbd5e1;
        border-radius: 0.5rem;
        max-width: 340px;
        margin: 0 auto;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    }

    .ticket-header {
        text-align: center;
        border-bottom: 2px solid #000;
        padding-bottom: 1rem;
        margin-bottom: 1rem;
        background: linear-gradient(
            to bottom,
            rgba(0, 0, 0, 0.02),
            transparent
        );
        padding-top: 0.5rem;
        border-radius: 4px 4px 0 0;
    }

    .ticket-logo {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
    }

    .ticket-title {
        font-size: 0.7rem;
        font-weight: 700;
        color: #555;
        margin-bottom: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.15em;
    }

    .ticket-business {
        font-size: 1.15rem;
        color: #000;
        font-weight: 900;
        margin-bottom: 0.6rem;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        line-height: 1.2;
    }

    .ticket-business-info {
        font-size: 0.7rem;
        color: #444;
        line-height: 1.7;
        margin: 0.3rem 0;
        font-style: italic;
    }

    .ticket-separator {
        border: none;
        border-top: 1px dashed #333;
        margin: 0.9rem 0;
        position: relative;
    }

    .ticket-separator::before,
    .ticket-separator::after {
        content: "";
        position: absolute;
        top: -1px;
        width: 8px;
        height: 1px;
        background: #333;
    }

    .ticket-separator::before {
        left: 0;
    }

    .ticket-separator::after {
        right: 0;
    }

    .ticket-separator.double {
        border-top: 3px double #000;
        margin: 1rem 0;
    }

    .ticket-info-section {
        background: rgba(0, 0, 0, 0.02);
        padding: 0.6rem;
        border-radius: 4px;
        margin: 0.75rem 0;
    }

    .ticket-info-row {
        display: flex;
        justify-content: space-between;
        font-size: 0.72rem;
        color: #000;
        margin: 0.35rem 0;
        line-height: 1.5;
        padding: 0.15rem 0;
    }

    .ticket-info-row span:first-child {
        font-weight: 600;
        color: #555;
        text-transform: uppercase;
        font-size: 0.68rem;
        letter-spacing: 0.05em;
    }

    .ticket-info-row span:last-child {
        font-weight: 800;
        font-family: "Courier New", monospace;
    }

    .ticket-date {
        font-size: 0.7rem;
        color: #000;
        margin-top: 0.5rem;
    }

    .ticket-items {
        margin: 0.9rem 0;
        background: rgba(0, 0, 0, 0.01);
        padding: 0.5rem;
        border-radius: 4px;
    }

    .ticket-items-header {
        display: grid;
        grid-template-columns: 2fr 0.9fr 1fr 1.3fr;
        gap: 0.3rem;
        font-size: 0.68rem;
        font-weight: 800;
        color: #000;
        padding: 0.4rem 0.3rem;
        border-bottom: 2px solid #000;
        margin-bottom: 0.4rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        background: rgba(0, 0, 0, 0.05);
    }

    .ticket-item {
        display: grid;
        grid-template-columns: 2fr 0.9fr 1fr 1.3fr;
        gap: 0.3rem;
        padding: 0.5rem 0.3rem;
        font-size: 0.74rem;
        color: #000;
        border-bottom: 1px dotted #d0d0d0;
        transition: background 0.15s;
    }

    .ticket-item:last-child {
        border-bottom: none;
    }

    .ticket-item:hover {
        background: rgba(0, 0, 0, 0.02);
    }

    .ticket-item-name {
        font-weight: 700;
        word-wrap: break-word;
        overflow-wrap: break-word;
        text-transform: capitalize;
        line-height: 1.3;
    }

    .ticket-item-qty,
    .ticket-item-unit-price,
    .ticket-item-price {
        text-align: right;
        font-family: "Courier New", monospace;
        font-weight: 600;
    }

    .ticket-item-price {
        font-weight: 800;
    }

    .ticket-totals {
        border-top: 2px solid #000;
        padding-top: 0.9rem;
        margin-top: 0.9rem;
        background: rgba(0, 0, 0, 0.02);
        padding: 0.8rem 0.5rem 0.5rem;
        border-radius: 4px;
    }

    .ticket-total-row {
        display: flex;
        justify-content: space-between;
        padding: 0.35rem 0;
        font-size: 0.82rem;
        color: #000;
        align-items: center;
    }

    .ticket-total-row span:first-child {
        font-weight: 700;
        text-transform: uppercase;
        font-size: 0.75rem;
        letter-spacing: 0.05em;
    }

    .ticket-total-row span:last-child {
        font-weight: 800;
        font-family: "Courier New", monospace;
        font-size: 0.9rem;
    }

    .ticket-total-row.subtotal {
        font-size: 0.78rem;
        color: #444;
    }

    .ticket-total-row.subtotal span:last-child {
        font-size: 0.82rem;
    }

    .ticket-total-row.final {
        font-size: 1.3rem;
        font-weight: 900;
        color: #000;
        margin-top: 0.7rem;
        padding: 0.8rem 0.4rem 0.5rem;
        border-top: 3px double #000;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        background: rgba(0, 0, 0, 0.08);
        border-radius: 4px;
    }

    .ticket-total-row.final span:last-child {
        font-size: 1.4rem;
    }

    .ticket-payment-info {
        margin-top: 0.9rem;
        padding: 0.7rem 0.5rem;
        border-top: 1px solid #999;
        border-bottom: 1px solid #999;
        background: linear-gradient(
            to bottom,
            rgba(0, 0, 0, 0.02),
            transparent
        );
    }

    .ticket-barcode {
        text-align: center;
        margin: 1.2rem 0 0.8rem;
        padding: 0.8rem 0.5rem;
        background: white;
        border: 2px solid #000;
        border-radius: 4px;
    }

    .ticket-barcode-lines {
        display: flex;
        justify-content: center;
        gap: 1px;
        margin-bottom: 0.4rem;
        padding: 0.3rem;
        background: white;
    }

    .ticket-barcode-line {
        width: 2px;
        height: 45px;
        background: #000;
        border-radius: 1px;
    }

    .ticket-barcode-line.thick {
        width: 4px;
    }

    .ticket-barcode-number {
        font-size: 0.7rem;
        color: #000;
        font-weight: 800;
        letter-spacing: 0.2em;
        font-family: "Courier New", monospace;
        margin-top: 0.2rem;
    }

    .ticket-footer {
        text-align: center;
        margin-top: 1rem;
        padding-top: 1rem;
        border-top: 2px solid #000;
        font-size: 0.7rem;
        color: #000;
        line-height: 1.9;
    }

    .ticket-footer-thanks {
        font-weight: 900;
        text-transform: uppercase;
        margin-bottom: 0.5rem;
        font-size: 0.95rem;
        letter-spacing: 0.12em;
        color: #000;
    }

    .ticket-footer-message {
        font-style: italic;
        font-weight: 600;
        margin-bottom: 0.7rem;
        font-size: 0.75rem;
    }

    .ticket-footer-wave {
        font-size: 1.2rem;
        margin: 0.5rem 0;
    }

    .ticket-footer-legal {
        font-size: 0.62rem;
        color: #777;
        margin-top: 0.8rem;
        padding-top: 0.6rem;
        border-top: 1px dashed #ccc;
        font-style: italic;
        line-height: 1.5;
    }

    .ticket-actions {
        display: flex;
        gap: 0.75rem;
        margin-top: 1.5rem;
    }

    .ticket-actions .btn {
        flex: 1;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }

    @keyframes slideDown {
        from {
            opacity: 0;
            transform: translateY(-20px) scale(0.95);
        }
        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }

    @media print {
        /* Ocultar absolutamente todo primero */
        body * {
            visibility: hidden !important;
            display: none !important;
        }

        body {
            background: white !important;
            margin: 0 !important;
            padding: 0 !important;
        }

        /* Ocultar explícitamente el layout */
        .app-layout,
        .navbar,
        .navbar *,
        .main-content,
        .container,
        .pos-container,
        .pos-grid {
            display: none !important;
            visibility: hidden !important;
        }

        /* Mostrar SOLO el modal del ticket */
        #ticket-modal {
            display: block !important;
            visibility: visible !important;
            position: fixed !important;
            top: 0 !important;
            left: 0 !important;
            right: 0 !important;
            bottom: 0 !important;
            background: white !important;
            padding: 0 !important;
            margin: 0 !important;
            z-index: 99999 !important;
        }

        #ticket-modal,
        #ticket-modal *,
        #ticket-content,
        #ticket-content * {
            visibility: visible !important;
            display: block !important;
        }

        #ticket-modal .modal-content {
            box-shadow: none !important;
            border: none !important;
            max-width: 100% !important;
            margin: 0 !important;
            padding: 0 !important;
            background: white !important;
        }

        /* Ocultar botones y cerrar */
        .modal-close,
        .ticket-actions,
        .modal-close *,
        .ticket-actions *,
        #close-ticket,
        #print-ticket,
        #new-sale,
        button {
            display: none !important;
            visibility: hidden !important;
            opacity: 0 !important;
            height: 0 !important;
            width: 0 !important;
            overflow: hidden !important;
        }

        /* Estilos del ticket para impresión */
        .ticket {
            position: static !important;
            border: none !important;
            box-shadow: none !important;
            margin: 0 auto !important;
            padding: 20px !important;
            background: white !important;
            max-width: 320px !important;
        }
    }
</style>

<script>
    import { api } from "../lib/api";

    const currentBusiness = JSON.parse(
        localStorage.getItem("currentBusiness") || "{}"
    );

    if (!currentBusiness.id) {
        window.location.href = "/businesses";
    }

    let products: any[] = [];
    let cart: any[] = [];
    let cashRegisterId: string | null = null;

    async function loadProducts(search = "") {
        try {
            products = await api.getProducts(currentBusiness.id, search);
            renderProducts();
        } catch (error) {
            console.error("Error loading products:", error);
        }
    }

    function renderProducts() {
        const container = document.getElementById("products-list");
        if (!container) return;

        container.innerHTML = products
            .map(
                (product) => `
      <div class="product-card" data-product-id="${product.id}" style="background: white; border: none; border-radius: 0; padding: 1.25rem; cursor: pointer; transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1); position: relative; user-select: none; box-shadow: none;">
        <div class="product-name" style="font-weight: 700; font-size: 1rem; color: #0f172a; margin-bottom: 0.75rem; line-height: 1.4; min-height: 2.8em; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">${product.name}</div>
        <div style="display: flex; justify-content: space-between; align-items: center; margin-top: auto;">
          <div class="product-price" style="font-weight: 800; font-size: 1.5rem; color: #6366f1; letter-spacing: -0.03em;">$${product.price.toFixed(2)}</div>
          <div class="product-stock" style="font-size: 0.75rem; color: #64748b; background: #f1f5f9; padding: 0.25rem 0.625rem; border-radius: 0.375rem; font-weight: 600;">📦 ${product.stock}</div>
        </div>
      </div>
    `
            )
            .join("");

        container.querySelectorAll(".product-card").forEach((card) => {
            const htmlCard = card as HTMLElement;

            // Eventos de hover
            htmlCard.addEventListener("mouseenter", () => {
                htmlCard.style.transform = "translateY(-8px) scale(1.05)";
                htmlCard.style.zIndex = "10";
            });

            htmlCard.addEventListener("mouseleave", () => {
                htmlCard.style.transform = "translateY(0) scale(1)";
                htmlCard.style.zIndex = "1";
            });

            htmlCard.addEventListener("click", () => {
                const productId = htmlCard.getAttribute("data-product-id");
                const product = products.find((p) => p.id === productId);
                if (product) {
                    addToCart(product);

                    // Visual feedback
                    htmlCard.classList.add("product-added");
                    setTimeout(
                        () => htmlCard.classList.remove("product-added"),
                        600
                    );
                }
            });
        });
    }

    function addToCart(product: any) {
        const existing = cart.find((item) => item.product_id === product.id);
        if (existing) {
            if (existing.quantity < product.stock) {
                existing.quantity++;
            }
        } else {
            if (product.stock > 0) {
                cart.push({
                    product_id: product.id,
                    name: product.name,
                    unit_price: product.price,
                    quantity: 1,
                });
            }
        }
        renderCart();
    }

    function renderCart() {
        const container = document.getElementById("cart-items");
        if (!container) return;

        if (cart.length === 0) {
            container.innerHTML = `
                <div class="empty-cart">
                    <div class="empty-cart-icon">🛒</div>
                    <div class="empty-cart-text">Carrito vacío</div>
                    <div class="empty-cart-hint">Selecciona productos para agregar</div>
                </div>
            `;
        } else {
            container.innerHTML = cart
                .map(
                    (item, index) => `
        <div class="cart-item">
          <div class="cart-item-info">
            <div class="cart-item-name">${item.name}</div>
            <div class="cart-item-price">$${item.unit_price.toFixed(2)}</div>
          </div>
          <div class="cart-item-qty">
            <button class="qty-btn" data-action="decrease" data-index="${index}">-</button>
            <span>${item.quantity}</span>
            <button class="qty-btn" data-action="increase" data-index="${index}">+</button>
          </div>
          <button class="btn-danger btn-sm" data-action="remove" data-index="${index}">×</button>
        </div>
      `
                )
                .join("");

            // Add event listeners
            container.querySelectorAll("[data-action]").forEach((btn) => {
                btn.addEventListener("click", (e) => {
                    const target = e.currentTarget as HTMLElement;
                    const action = target.getAttribute("data-action");
                    const index = parseInt(
                        target.getAttribute("data-index") || "0"
                    );

                    if (action === "decrease") {
                        if (cart[index].quantity > 1) {
                            cart[index].quantity--;
                        }
                    } else if (action === "increase") {
                        cart[index].quantity++;
                    } else if (action === "remove") {
                        cart.splice(index, 1);
                    }

                    renderCart();
                });
            });
        }

        updateTotals();
    }

    function updateTotals() {
        const subtotal = cart.reduce(
            (sum, item) => sum + item.quantity * item.unit_price,
            0
        );
        const discount = parseFloat(
            (document.getElementById("discount") as HTMLInputElement)?.value ||
                "0"
        );
        const total = subtotal - discount;

        document.getElementById("subtotal")!.textContent =
            `$${subtotal.toFixed(2)}`;
        document.getElementById("total")!.textContent = `$${total.toFixed(2)}`;

        const completeBtn = document.getElementById(
            "complete-sale-btn"
        ) as HTMLButtonElement;
        completeBtn.disabled = cart.length === 0;
    }

    async function checkCashRegister() {
        try {
            const cashRegister = await api.getCurrentCash(currentBusiness.id);
            cashRegisterId = cashRegister.id;
        } catch (error) {
            // No open cash register
            cashRegisterId = null;
        }
    }

    document
        .getElementById("product-search")
        ?.addEventListener("input", (e) => {
            const search = (e.target as HTMLInputElement).value;
            loadProducts(search);
        });

    document
        .getElementById("discount")
        ?.addEventListener("input", updateTotals);

    function showTicket(saleData: any) {
        const subtotal = cart.reduce(
            (sum, item) => sum + item.unit_price * item.quantity,
            0
        );
        const discount = parseFloat(
            (document.getElementById("discount") as HTMLInputElement).value ||
                "0"
        );
        const total = subtotal - discount;
        const now = new Date();

        // Formatear fecha y hora
        const fecha = now.toLocaleDateString("es-ES", {
            day: "2-digit",
            month: "2-digit",
            year: "numeric",
        });
        const hora = now.toLocaleTimeString("es-ES", {
            hour: "2-digit",
            minute: "2-digit",
            second: "2-digit",
        });

        // Número de ticket
        const ticketNumber = Date.now().toString().slice(-8);

        // Generar código de barras simulado (líneas aleatorias)
        const barcodeLines = Array(30)
            .fill(0)
            .map(() => {
                const rand = Math.random();
                if (rand < 0.3)
                    return '<div class="ticket-barcode-line thick"></div>';
                if (rand < 0.6)
                    return '<div class="ticket-barcode-line"></div>';
                return '<div class="ticket-barcode-line" style="width: 1px;"></div>';
            })
            .join("");

        const ticketHTML = `
            <div class="ticket">
                <div class="ticket-header">
                    <div class="ticket-logo">🏪</div>
                    <div class="ticket-title">Comprobante de Venta</div>
                    <div class="ticket-business">${currentBusiness.name}</div>
                    <div class="ticket-business-info">
                        📍 Calle Principal #123<br>
                        📞 (555) 123-4567<br>
                        🆔 RFC/CUIT: XX-XXXXX-XXX
                    </div>
                </div>
                
                <div class="ticket-separator"></div>
                
                <div class="ticket-info-section">
                    <div class="ticket-info-row">
                        <span>Ticket No.:</span>
                        <span>#${ticketNumber}</span>
                    </div>
                    <div class="ticket-info-row">
                        <span>Fecha:</span>
                        <span>${fecha}</span>
                    </div>
                    <div class="ticket-info-row">
                        <span>Hora:</span>
                        <span>${hora}</span>
                    </div>
                    <div class="ticket-info-row">
                        <span>Atendió:</span>
                        <span>Usuario POS</span>
                    </div>
                </div>
                
                <div class="ticket-separator"></div>
                
                <div class="ticket-items">
                    <div class="ticket-items-header">
                        <div><strong>Producto</strong></div>
                        <div style="text-align: right;"><strong>Cant</strong></div>
                        <div style="text-align: right;"><strong>P.Unit</strong></div>
                        <div style="text-align: right;"><strong>Importe</strong></div>
                    </div>
                    ${cart
                        .map(
                            (item) => `
                        <div class="ticket-item">
                            <div class="ticket-item-name">${item.name}</div>
                            <div class="ticket-item-qty">${item.quantity}</div>
                            <div class="ticket-item-unit-price">$${item.unit_price.toFixed(2)}</div>
                            <div class="ticket-item-price"><strong>$${(item.unit_price * item.quantity).toFixed(2)}</strong></div>
                        </div>
                    `
                        )
                        .join("")}
                </div>
                
                <div class="ticket-totals">
                    <div class="ticket-total-row subtotal">
                        <span>Subtotal</span>
                        <span>$${subtotal.toFixed(2)}</span>
                    </div>
                    ${
                        discount > 0
                            ? `
                    <div class="ticket-total-row subtotal">
                        <span>⚡ Descuento</span>
                        <span style="color: #c00;">-$${discount.toFixed(2)}</span>
                    </div>
                    `
                            : ""
                    }
                    <div class="ticket-total-row final">
                        <span>💰 Total</span>
                        <span>$${total.toFixed(2)}</span>
                    </div>
                </div>
                
                <div class="ticket-payment-info">
                    <div class="ticket-info-row">
                        <span>Forma de Pago:</span>
                        <span>${saleData.payment_method}</span>
                    </div>
                    <div class="ticket-info-row">
                        <span>Cantidad Recibida:</span>
                        <span>$${total.toFixed(2)}</span>
                    </div>
                    <div class="ticket-info-row">
                        <span>Cambio:</span>
                        <span>$0.00</span>
                    </div>
                </div>
                
                <div class="ticket-barcode">
                    <div class="ticket-barcode-lines">
                        ${barcodeLines}
                    </div>
                    <div class="ticket-barcode-number">${ticketNumber}</div>
                </div>
                
                <div class="ticket-footer">
                    <div class="ticket-footer-thanks">
                        ¡Gracias por su compra!
                    </div>
                    <div class="ticket-footer-message">💙 Vuelva pronto 💙</div>
                    <div class="ticket-footer-wave">⭐ ⭐ ⭐</div>
                    <div class="ticket-footer-legal">
                        Este documento no es válido como<br>
                        comprobante fiscal<br>
                        • Conserve este ticket •
                    </div>
                </div>
            </div>
        `;

        document.getElementById("ticket-content")!.innerHTML = ticketHTML;
        document.getElementById("ticket-modal")!.style.display = "flex";
    }

    document
        .getElementById("complete-sale-btn")
        ?.addEventListener("click", async () => {
            if (cart.length === 0) return;

            try {
                const discount = parseFloat(
                    (document.getElementById("discount") as HTMLInputElement)
                        .value || "0"
                );
                const paymentMethod = (
                    document.getElementById(
                        "payment-method"
                    ) as HTMLSelectElement
                ).value;

                const saleData = {
                    business_id: currentBusiness.id,
                    items: cart,
                    discount,
                    payment_method: paymentMethod,
                    cash_register_id: cashRegisterId,
                };

                await api.createSale(saleData);

                showTicket(saleData);
            } catch (error: any) {
                alert("Error al completar la venta: " + error.message);
            }
        });

    document.getElementById("close-ticket")?.addEventListener("click", () => {
        document.getElementById("ticket-modal")!.style.display = "none";
    });

    document.getElementById("new-sale")?.addEventListener("click", () => {
        document.getElementById("ticket-modal")!.style.display = "none";
        cart = [];
        renderCart();
        loadProducts();
        (document.getElementById("discount") as HTMLInputElement).value = "0";
    });

    document.getElementById("print-ticket")?.addEventListener("click", () => {
        window.print();
    });

    checkCashRegister();
    loadProducts();
</script>
